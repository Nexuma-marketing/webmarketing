# 6-2 — Error Analysis Log

Working log for issues raised on/around 2026-06-02. For each item:
- **Symptom**: exactly what the client / tester saw
- **Root cause**: why it happened (with file:line references when relevant)
- **Verdict**: real bug vs expected behavior vs config issue vs user action
- **Action**: code change, SQL, or "no change — explanation only"

Behavior contract for this session:
- Diagnose first. Don't edit code unless the root cause is a verified code defect.
- "User already registered", missing leads, etc. usually aren't code bugs — they are differences between auth.users / profiles / leads tables. Explain instead of patch.
- When a real code defect IS identified, fix it, commit it, and record the commit hash in the entry.
- After the user verifies the fix works, flip the entry's Status to "Verified".

Each entry uses this structure:
- **#NN — short title**
- **Symptom** — what the client saw
- **Root cause** — why it happened, with file:line refs
- **Verdict** — real bug / expected behavior / config / user action
- **Action** — what was done (or why nothing was done)
- **Status** — Analyzed (explanation only) · Fix shipped in <commit> · Verified by user

---

## Entries

### #01 — `alexsmarke@gmail.com` says "User already registered" but is not in `/admin/leads` export

**Symptom** (Alex, 2026-06-01)
> "Porque este mail alexsmarke@gmail.com dice que existe, si no aparece en la lista de leads ni la de usuario? aún no se ha limpiado bien la base de datos?"

The signup form rejects `alexsmarke@gmail.com` with the red "User already registered" banner. Yet the leads export (`leads_export_2026-06-01.xlsx`) does not contain that email.

**Root cause**

Supabase splits user state across three tables:

| Table | Stores | Populated by |
|---|---|---|
| `auth.users` | Email + password | `supabase.auth.signUp()` in `/register` |
| `profiles` | Name, role, phone | Trigger on `auth.users` insert |
| `leads` | Marketing pipeline rows | `/api/leads` — called ONLY from inside the form wizards (owner_form, tenant_preferences, pymes_diagnosis), NOT from `/register` |

The "User already registered" check (`supabase.auth.signUp`) reads `auth.users` only. It is unaware of the leads table.

`alexsmarke@gmail.com` exists in `auth.users` because someone registered with it after v32/v33 ran (the cleanup migrations removed the original 20 test rows on 2026-05-22). The new registration never reached the form submit step, so no `/api/leads` call happened and no leads row was created. Result: the email appears registered but is absent from the leads export.

**Verdict**

**Not a code bug. Not a cleanup-migration bug.** Working as designed:
- /register correctly persists to auth.users
- /admin/leads correctly only lists form-completed leads
- The leads export reflects /admin/leads, so it correctly omits register-only accounts

**Action**

No code change. To remove this specific account (if the client wants the email free again):

```sql
DELETE FROM profiles WHERE LOWER(email) = 'alexsmarke@gmail.com';
DELETE FROM auth.users WHERE LOWER(email) = 'alexsmarke@gmail.com';
```

For Alex's understanding, point her at the 3-table model (auth.users vs profiles vs leads).

**Status**: Analyzed — no code change required.

**Update 2026-06-03** — Alex re-reported: *"Pendiente 3 Junio: Sigue igual. Pendiente 1 June: Porque este mail alexsmarke@gmail.com dice que existe, si no aparece en la lista de leads ni la de usuario? aún no se ha limpiado bien la base de datos?"*

The root cause is the same 3-table model explained above, but the most likely current state is now: `auth.users` has the row → signup rejects → but `profiles` row was already deleted (in v32 or by manual cleanup) → so /admin/users also shows nothing. The result is an "orphan" auth.users record that blocks signup but is invisible in both admin panels.

Diagnostic SQL (run to confirm the state):

```sql
SELECT 'auth.users' AS source, id, email, created_at, last_sign_in_at
FROM auth.users WHERE LOWER(email) = 'alexsmarke@gmail.com'
UNION ALL
SELECT 'profiles' AS source, id, email, created_at, NULL
FROM profiles WHERE LOWER(email) = 'alexsmarke@gmail.com';
```

Expected: 1 row from `auth.users` + 0 rows from `profiles` → confirms the orphan state.

Cleanup SQL (run to remove the orphan permanently):

```sql
DELETE FROM auth.users WHERE LOWER(email) = 'alexsmarke@gmail.com';
```

After running, signup with `alexsmarke@gmail.com` will work normally. This is a one-time data cleanup for this specific email — NOT a recurring issue. Every email that ever passes through `/register` creates an auth.users row, so the same situation will recur if anyone tests with an email that later gets manually purged from profiles only.

**Resolution 2026-06-03**

Diagnostic SQL (Tony, 6/3) showed the actual state was different from initially expected:
- `auth.users` had a row created **2026-05-23** (1 day AFTER v33 cleanup of 5/22) — last_sign_in_at 2026-05-27.
- `profiles` ALSO had a row, same UUID `4118b7bf-...`, `full_name = "Prueba property F4"`, `role = "propietario"`.
- 2 payment rows in `/admin/payments` ($200 Low Price + $200 Founder Package) attributed to this profile, both from 2026-05-27.

So `alexsmarke@gmail.com` had been re-registered after v33 by someone (most likely Alex herself during testing — browser autofill of an older form session) and was actively a customer account named "Prueba property F4". Alex didn't recognize it because she was searching `/admin/users` by the *email* but the row is displayed under the *full_name* "Prueba property F4".

Tony ran:

```sql
DELETE FROM profiles WHERE LOWER(email) = 'alexsmarke@gmail.com';
DELETE FROM auth.users WHERE LOWER(email) = 'alexsmarke@gmail.com';
```

Both reported "Success. No rows returned". The email is now fully free. The 2 payment rows remain in `/admin/payments` with `user_id = NULL` (via FK SET NULL) — total revenue still correct, just the customer column shows "Unknown" for those 2 rows. Alex was informed and approved the orphaning of those 2 payment rows in exchange for the email being free.

**Status**: ✅ **Verified 2026-06-03**. SQL cleanup ran successfully. Email now free for re-registration.

---

### #02 — Customer email still not arriving after Resend domain was verified

**Symptom** (Alex, 2026-06-01)
> "Esta igual sin ajuste, cree un usuario nuevo desde el formulario (Crear cuenta gratis) de la página inicial, pero no llego mail al cliente, solo llego al comercial. Se ha probado con otros correos y pasa lo mismo."

Test registered `jacreingenieria@gmail.com` as a Property Owner. Commercial notification arrived at `alexsanabria33@hotmail.com` ✅. Customer confirmation to `jacreingenieria@gmail.com` did NOT arrive ❌ (neither inbox nor spam — the May confirmation emails are still visible in the inbox but no June 1 one).

Critical clue in screenshot 1:

The email header in Outlook shows:
> **From: WebMarketing <onboarding@resend.dev>**
> Date: Mon 2026-06-01 1:20 PM

**Root cause**

The Vercel environment variables `RESEND_API_KEY` and `RESEND_FROM_EMAIL` have NOT been updated yet:

1. `RESEND_FROM_EMAIL` is still `WebMarketing <onboarding@resend.dev>` — the proof is the From field in the screenshot. If it had been updated to `Nexuma Marketing <notifications@nexuma.ca>`, that's what the header would show.

2. Because the from address still points at `onboarding@resend.dev` (Resend's shared test domain), Gmail's spam filters silently drop the customer-side send. Hotmail/Outlook is more lenient with shared domains — which is why the commercial copy at `alexsanabria33@hotmail.com` arrives but the Gmail customer copy does not.

The domain `nexuma.ca` IS verified in Alex's Resend account (confirmed by previous screenshot showing "Domain verified"). The DNS is correct. The ONLY thing missing is the Vercel side: pointing the app at Alex's API key + at the verified from address.

Verifiable at `/api/health` → `email.fromAddress` field. Right now it still shows the old value; once env vars are updated, it will show `Nexuma Marketing <notifications@nexuma.ca>`.

**Verdict**

**Not a code bug. Config gap.** Code is already correct (commit `4d27a79` made all defaults "Nexuma Marketing"). The two production env vars in Vercel must be swapped:

| Env var | Current value | Target value |
|---|---|---|
| `RESEND_API_KEY` | Old key (different Resend account) | New `re_...` key from Alex's Resend account |
| `RESEND_FROM_EMAIL` | `WebMarketing <onboarding@resend.dev>` | `Nexuma Marketing <notifications@nexuma.ca>` |

After both are updated and Vercel redeploys, customer emails will start arriving from a verified-domain sender and Gmail will accept them.

**Action**

No code change. Tony to update the two Vercel env vars and redeploy. Sequence:

1. Log into Resend with Alex's credentials → API keys → create `nexuma-production` with domain scope `nexuma.ca` → copy the `re_...` key
2. Vercel → Settings → Environment Variables → edit both vars (Production + Preview + Development)
3. Deployments → ⋮ on latest → Redeploy (uncheck build cache)
4. Verify `/api/health` shows the new `fromAddress`
5. Register a test user with a Gmail address and confirm both emails arrive

**Status**: Analyzed — pending Vercel env-var update (not a code defect).

---

### #03 — May 26 evidence: customer email not arriving via either entry path

**Symptom** (Alex, dated 2026-05-26 — predates the domain verification on 5/29)

Two distinct landing-page entry paths, both with the same complaint:

1. **"Create Free Account" hero button** → registered `produccionulf@gmail.com` as `propietario`, completed signup. Annotation: *"Cree desde aca, No llega e-mail al Comercial."* Gmail inbox + spam folder both empty for that email.

2. **"Property Owners" / "Business Owners" hero buttons** → annotation: *"Entrando por aca pide los datos del cliente al final pero no llegan los e-mails."* (User filled the form to the end; no email reached the customer.)

Original docx wording: *"Pendiente 26 Mayo: Cree un usuario nuevo desde el formulario (Crear cuenta gratis) de la página inicial, pero no llego mail al cliente, solo llego al comercial."*

**Root cause**

**Identical to #02.** The screenshots are dated 26 May — 3 days BEFORE the `nexuma.ca` domain was verified in Resend (29 May). At that time:
- `RESEND_FROM_EMAIL` = `WebMarketing <onboarding@resend.dev>` (Resend's shared test sender)
- Gmail silently drops emails from that shared domain
- Hotmail/Outlook (alexsanabria33@hotmail.com) is more lenient → commercial copy arrived
- Gmail (produccionulf@gmail.com, jacreingenieria@gmail.com) → customer copy dropped

Two sub-paths to note about the diagnosis:

(a) **"Create Free Account" path**: `/register` itself sends NO email (by design — only the form-completion routes send to commercial + customer). When Alex said "Cree desde aca, no llega e-mail al Comercial", she was reporting on the FULL flow: register → redirect to wizard → complete form → submit. That submit is what triggers `/api/owner-submit-email`. So the absence of email is the same form-submit email path, not a missing-register-email path.

(b) **"Property Owners" / "Business Owners" path**: same wizard, same form-submit email. Plus: if the visitor wasn't logged in BEFORE clicking these buttons, the old code (pre-`f54a07a`) silently lost the form data on submit. Fixed in commit `f54a07a` (hero buttons now go to `/register?role=…` first).

**Verdict**

**Not a separate bug.** Two distinct reports rolling up to the same two root causes:
- Customer email deliverability — same as #02 (Resend env vars)
- Anonymous-form-submit data loss — already fixed in `f54a07a` (May 27)

**Action**

No new code change. Resolution path is identical to #02: update the Vercel env vars and redeploy. Verifying #02 will automatically resolve #03.

**Status**: Analyzed — duplicate of #02. Will close when #02 is verified.

---

### #04 — Form-submission email to client still missing (May 22 → Jun 1 timeline)

**Symptom** (Alex, escalation log across three dates)

Item 3 of the docx — "Formularios y perfilamiento más envíos de email a comercial y clientes":

| Date | What Alex reported |
|---|---|
| 22 May | "Se completó el brief de propietario, pero no llego ningun correo ni al comercial, ni al cliente." |
| 26 May | "Se completó el brief de propietario, pero no llegó ningún correo al cliente, solo al comercial." |
| **1 Jun** | **"Esta igual, no llegan los mail a los clientes."** |

Screenshots show the Gmail inbox of `produccionulf@gmail.com` (a tester / customer) with no email from Nexuma after completing the Owner Discovery Brief, plus the resulting property in the user's `/dashboard/properties` view (proving the form submission itself succeeded — only the customer email failed).

**Root cause**

**Identical to #02 and #03.** Email pipeline status by date:

| Date | RESEND_FROM_EMAIL in Vercel | Commercial side | Customer side |
|---|---|---|---|
| 22 May | `WebMarketing <notifications@nexuma.ca>` (unverified domain — Resend rejects both) | ❌ | ❌ |
| 26 May | switched to `WebMarketing <onboarding@resend.dev>` (shared test domain) | ✅ Hotmail accepts | ❌ Gmail drops |
| 1 Jun | still `WebMarketing <onboarding@resend.dev>` — env vars NOT updated even after domain verification on 5/29 | ✅ Hotmail accepts | ❌ Gmail drops |

The form submission code (`/api/owner-submit-email`) has been correct the entire time. It correctly issues two `resend.emails.send()` calls — one to commercial, one to the customer. The customer call is rejected at the **delivery layer** by Gmail (not by our code, not by Resend's API) because the FROM address has no DKIM reputation tied to the app.

The persistence from 22 May → 1 Jun is explained by:
- 22→26: switched FROM to onboarding@resend.dev — fixed commercial half
- 26→29: client added DNS records to Cloudflare → Resend verified `nexuma.ca`
- 29→1 Jun: Vercel env vars were never updated, so production still uses the old sender → customer half stays broken

**Verdict**

**Not a code defect.** The form-email code path is identical to commit `3dd6278` and is correct. Resolution = same env-var update pending under #02.

**Action**

No new code change. Resolves when `RESEND_API_KEY` + `RESEND_FROM_EMAIL` are updated in Vercel and the app redeploys.

**Status**: Analyzed — duplicate of #02. Will close when #02 is verified.

---

### #05 — Hero CTA edit fields ("Property Owners" / "Business Owners") missing in /admin/content

**Symptom** (Alex, 1 Jun, escalation from 26 May)
> "Pendiente 1 Jun: Está igual aun falta los campos de edición solicitados desde el 26 de Mayo y que antes existían."
>
> "Pendiente 26 Mayo: Faltan campos de edición, antes en hero se podían cambiar los dos nombres del CTA de la pagina principal los que estan abajo de Grow your property & Business, especificamente: Property Owner y el de Business owners."

Screenshot shows /admin/content with section dropdown set to `landing_hero`, only 3 items visible (`hero_cta`, `hero_subtitle`, `hero_title`), and the annotation "Faltan campos de CTA" pointing at the empty space where the Property Owner / Business Owner button labels should be.

**Root cause**

A real code defect introduced by an incomplete fix on May 27.

Commit `f54a07a` (May 27) added two new keys to the rendering layer:

```ts
heroCTAOwner: pickHeadline(headlineRows, "hero_cta_owner", "Property Owners"),
heroCTABusiness: pickHeadline(headlineRows, "hero_cta_business", "Business Owners"),
```

`headlineRows` is filtered from `site_content` where `section = "landing_headlines"`. So the code reads those keys correctly — but only IF an admin has saved a row with that key.

The defect: `src/app/(dashboard)/admin/content/page.tsx` `SECTIONS[landing_headlines].starter` was never updated to include those two keys. Result:

- Admin opens /admin/content → "Landing Page - Section Headlines" section
- Sees the existing headlines (How it Works, Services, etc.) but NO row labeled `hero_cta_owner` or `hero_cta_business`
- "Add New Content Item" lets them create it manually but the key name isn't documented anywhere → admin doesn't know to use exactly `hero_cta_owner`
- So in practice the rendering code always falls back to the hardcoded defaults ("Property Owners" / "Business Owners") and the labels look uneditable

Compounding issue: the page's default `selectedSection` was hardcoded to `"landing_hero"` — a section name that does NOT exist in `SECTIONS`. So an admin opening /admin/content lands on an unhinted, unlabeled view filled with the 3 orphan rows (`hero_cta`, `hero_subtitle`, `hero_title`) that were left over from before the section was retired.

**Verdict**

**Real code defect.** Fix shipped in commit `fd7a641` (this session).

**Action** (already done)

1. Added `hero_cta_owner` and `hero_cta_business` as the first two starter entries of the `landing_headlines` section in `admin/content/page.tsx`, each with helper text explaining which colored button it labels.
2. Renamed section label from "Section Headlines" to "**Headlines & Hero CTAs**" and updated the hint so an admin lands here and immediately sees that hero buttons are editable.
3. Changed default `selectedSection` from `"landing_hero"` to `"landing_headlines"` so the page opens with a valid section selected.

Once Vercel finishes deploying `fd7a641`, the admin will:
- Open `/admin/content` → see "Landing Page - Headlines & Hero CTAs" selected by default
- Find `hero_cta_owner` and `hero_cta_business` at the top of the list, ready to edit
- Save any new label → public hero buttons render the new label after a hard refresh

**Note about the orphan rows** (`hero_cta`, `hero_subtitle`, `hero_title` in `section="landing_hero"`): these are leftovers from before the section was retired in May. They are not read by any code path. The admin can safely delete them from `/admin/content` once they switch into the (now correctly selected by default) `landing_headlines` section instead — or leave them; they don't affect anything.

**Status**: ✅ **Verified by user 2026-06-02**. After Vercel rebuilt at `5e3598e`, /admin/content defaults to "Landing Page - Headlines & Hero CTAs" section with the new hint text. User added `hero_cta_owner` = "Property Owners" and `hero_cta_business` = "Business Owners" via the Add Item form — section count moved 14 → 16, both rows visible. Fix `fd7a641` confirmed working in production.

---

### #06 — "Cuando se entra por aca: no se está creando el cliente ni queda en el panel administrador"

**Symptom** (Alex, in the same Item 4 block, with a screenshot of the Sign In page failing for `alexsmarke@gmail.com`)
> "Pendiente: Cuando se entra por aca: no se está creando el cliente ni queda en el panel administrador."
>
> Annotation: "Con este se hizo un pago pero creo al cliente NO" (a payment was made with this account but the client wasn't created)

Screenshot shows the public Sign In page rejecting `alexsmarke@gmail.com` with "Invalid email or password".

**Root cause**

Two separate things layered on top of each other:

1. **The "client not created" half is already fixed** in commit `f54a07a` (May 27). Before that commit, the hero "Property Owners" / "Business Owners" buttons sent anonymous visitors directly to `/forms/propietario` or `/forms/pymes`. The form's submit handler called `supabase.auth.getUser()` and silently redirected to `/login` when no session existed → all 6 steps of form data were lost → no lead was ever written. After `f54a07a`, those buttons go to `/register?role=…` first, so the user creates an account before the form runs.

2. **The `alexsmarke@gmail.com` Sign In failure** is unrelated — that email was deleted from `auth.users` in migration v33 (May 22) and then deleted again in any subsequent cleanup attempts. "Invalid email or password" means there is no account with that email currently, OR the password is wrong. See #01 — this is the same `alexsmarke@gmail.com` confusion. If someone re-registered it during testing, the credentials they used then are the credentials Sign In expects now.

**Verdict**

**Not a current code defect.** The architectural fix landed in `f54a07a` on May 27. The May 26 screenshot in the docx predates that commit. The June 1 retest of the same email failing is a test-data hygiene issue, not a code issue (see #01).

**Action**

No code change. The "client not created" symptom should not reproduce on accounts created AFTER May 27. If Alex retests by registering a fresh email today, the flow is:
- Land on `/` → click "Property Owners"
- Redirect to `/register?role=propietario` with the User Type dropdown pre-selected
- Complete registration → automatic redirect to `/forms/propietario` with active session
- Complete 6 steps of brief → submit → both `discovery_briefs` and `leads` rows are written
- Admin sees the new lead in `/admin/leads`

**Status**: Analyzed — already fixed in `f54a07a`. Awaiting client verification on a fresh test account.

---

### #07 — Team assignment cannot be tested because emails don't arrive

**Symptom** (Alex, on Item 5 — Gestión de equipos)
> "Pendiente 1 Jun crear y asignar team: Cuando se solucione el envío de email pruebo la función de asignar equipos ya que no pude porque no llegan email para poder verificar nuevas asignaciones."
>
> "Pendiente 26 mayo crear y asignar team: Cuando se solucione el envio de email pruebo la función de asignar equipos ya que no pude porque no llegan email para poder verificar nuevas asignaciones."

Alex says she cannot test the **team-management feature** (`/admin/team` — create Admin / Marketing / Sales / Support internal users and assign roles) because the invited team members never receive their invitation email. Both the 26 May and 1 Jun reports are word-for-word identical.

**Root cause**

**Identical to #02.** The current internal-user creation flow in `src/app/api/admin/create-internal-user/route.ts`:

1. Admin opens `/admin/team` → "+ Add team member" → fills form (name, email, role, generated password)
2. Backend calls `admin.auth.admin.createUser({ email, password, email_confirm: true, user_metadata: { full_name, role } })`
3. `email_confirm: true` means Supabase Auth marks the email as already confirmed — Supabase does NOT send any built-in confirmation email
4. The admin sees the generated credentials in the dialog and is expected to share them with the new team member out-of-band

So the **team-creation flow itself does not send any email at all**. There is no code path that emails an invitation. The "email never arrives" complaint Alex is describing is actually about *form-submission* emails or *registration-confirmation* emails of any kind — i.e., she cannot validate that the new team member's account works because she can't go through the public sign-up flow with their credentials and verify the welcome email pattern. Same root cause as #02 (Vercel env vars not updated → customer-side emails dropped by Gmail).

If/when the client expects a literal "team invitation email" (e.g., a link the team member clicks to set their own password), that is a NEW FEATURE not currently in the codebase — none of the email helpers (`sendPaymentReceiptEmail`, `sendContactNotification`, `owner-submit-email`, etc.) target the team-invitation case. The current design is: admin generates a strong password, shares it privately, team member changes it from `/dashboard/profile` after first login.

**Verdict**

**Not a code defect — config gap (same as #02).** The reason Alex is blocked is the upstream email-delivery issue, not anything specific to the team feature. The team UI (`/admin/team`), the role-based middleware/sidebar guards (commits `b50e780` + `fd7a641`), and the RLS enforcement at the DB layer (migration v9) have all been verified previously and work correctly.

**Action**

No code change. Resolves automatically when #02 is verified — at that point Alex can:

1. Create a Marketing user from `/admin/team`
2. Share the generated password with that user (or with herself if testing alone)
3. Open an incognito window → log in with the team member's credentials → confirm they reach `/admin/*` with the correct restricted sidebar
4. Submit a test owner brief as a different (customer) account → verify the commercial notification email reaches `alexsanabria33@hotmail.com` AND the customer confirmation reaches the test Gmail address

If, after #02 is verified, Alex separately reports that she needs a built-in "send invitation email to the new team member" feature, that becomes a new entry — but the current docx language doesn't ask for it.

**Status**: Analyzed — duplicate of #02. Will close when #02 is verified.

---

### #08 — Supabase walkthrough and ownership transfer pending

**Symptom** (Alex, on Item 6 — Prueba Supabase)
> "Pendiente 1 Jun: Steve aun no me has dicho cómo realizar estas pruebas y esta a tu propiedad, cuando me lo pasas?"
>
> "Pendiente 22 mayo de probar ya que en un chat me dijiste: 'Dado que Supabase es actualmente mi base de datos personal, creo que lo mejor será revisar una vez que el proyecto esté completamente terminado. En ese momento, te guiaré paso a paso.' Entonces aún está pendiente a tu guía Steve."

Two distinct asks bundled into Item 6:

1. **Guided walkthrough** of the Supabase Dashboard — specifically the `users` / `profiles` tables and the `property-images` Storage bucket.
2. **Ownership transfer** — the Supabase project (`nldnqvsbcyzcfbdpymsg`) currently sits in Tony's personal Supabase organization. The client wants it moved into her own organization so she controls billing, access, and backups.

**Root cause**

**Not a code issue. Handover deliverable still pending.** Both asks are operational, not technical:

(a) The walkthrough is partially covered in writing already:
- `HANDOVER_MANUAL.md` §5 — Supabase data dictionary (every table + RLS overview)
- `HANDOVER_MANUAL.md` §5.`property_images` — "How to view / manage uploaded property photos in Supabase Studio" subsection added in commit `ae3f11b`
- `HANDOVER_MANUAL.md` §6 — Environment variables

What's missing is the **live session** — Alex specifically remembers Steve saying "te guiaré paso a paso" once the project is complete. She wants the walkthrough in real time, not just docs.

(b) The ownership transfer requires Tony to:
- Invite `alexsanabria33@hotmail.com` to the Supabase organization that owns project `nldnqvsbcyzcfbdpymsg`, OR
- Use Supabase's "Transfer project" flow (Project Settings → General → Transfer project to a different organization). The destination organization must exist first — i.e., Alex must create a Supabase account and an organization before the transfer can complete.

Both steps are blocked on a joint session — Alex needs to be at her computer to (i) accept the invite / confirm the org, and (ii) walk through the Dashboard with Tony explaining each section.

**Verdict**

**No code defect. Joint-session task.** Code-side everything Alex would learn from the walkthrough is already documented in `HANDOVER_MANUAL.md`. The remaining work is purely operational: schedule the session, do the transfer, explain the panels live.

**Action**

No code change. Tony to:

1. Reply to Alex with 2-3 candidate time slots for a 60-minute handover session covering Item 6 plus Item 10 (domain switch) and Item 11a-e (other ownership transfers) — these are all blocked on the same kind of meeting.
2. Ask her to create a Supabase account at supabase.com BEFORE the session so the destination organization exists.
3. During the session: share screen → walk through `auth.users`, `profiles`, `leads`, `payments`, `property-images` bucket → then use Supabase's transfer flow to move the project to her organization.

**Status**: Analyzed — joint-session task. No code change required. Closes when handover session is held.

**Update 2026-06-03 — third escalation**

Alex re-asked verbatim: *"Pendiente 1 Jun: Steve aun no me has dicho cómo realizar estas pruebas y esta a tu propiedad, cuando me lo pasas?"*

Pattern: 22 May → 1 Jun → 3 Jun. Three "Pendiente" markers without a single concrete proposal from Tony's side. The risk now is not technical (the walkthrough material is all documented in `HANDOVER_MANUAL.md`) but **relationship**: Alex is starting to feel ignored on this specific item. Every other day of postponement makes this worse.

The right next action is no longer "wait to schedule" — it's "propose 3 concrete time slots within the next 5 days and let her pick one". That converts the open-ended "later" into a decision she can act on.

Alex also still needs to create the 3 destination accounts (GitHub, Vercel, Supabase) before the project transfer can be executed. Those can be created independently of any session, so the message asking for time slots should also ask her to create those accounts in parallel.

**Status (unchanged)**: Joint-session task. No code change required. Closes when handover session is held — Tony to propose slots immediately to stop the escalation. — "Pone a llenar dos veces el formulario de registro de propiedad"

**Symptom** (Alex, 1 Jun)
> "Al crear clientes por la pagina principal por el botón Property owner, pone a llenar dos veces el formulario de registro de propiedad (datos, subir fotos, etc), pasa esto: por el botón Property owner primero pone a llenar el formulario y luego pide los datos (nombre, mail, etc) pero al llenar los datos de la persona, vuelve a salir el formulario de registro de la propiedad y toca volver al llenar toda la información, esta mal, eso no estaba asi."

The visitor fills the property registration form once → is asked for personal data (name, email) → after providing them, the property form appears AGAIN → has to refill from scratch.

**Root cause**

**Real code defect.** Commit `f54a07a` (May 27) fixed the **HERO buttons** at the top of the landing page so that "Property Owners" / "Business Owners" route through `/register?role=...` first — eliminating the double-form problem there. But the page has THREE more public CTAs (lower down, in the Services section grid) that were overlooked and still linked directly to the forms:

| CTA text | Old href (broken) | Section |
|---|---|---|
| "Start Discovery Brief" | `/forms/propietario` | Services → Property Owners card |
| "Find your home" | `/register` (no role hint) | Services → Tenants card |
| "Calculate your sales leak" | `/forms/pymes` | Services → SMBs card |

The first and third reproduce exactly the symptom Alex described:

1. Anonymous visitor clicks "Start Discovery Brief"
2. Lands on `/forms/propietario` — fills 6 steps of brief
3. Clicks Submit → form's `onSubmit` calls `supabase.auth.getUser()` → returns `null`
4. `router.push("/login")` (or the new `/register`) — **all 6 steps of React state lost**
5. Visitor registers (or signs in)
6. After registration `/register` redirects role=propietario → `/forms/propietario` AGAIN
7. Empty form, has to refill

The Tenants CTA "Find your home" works (it goes to /register) but doesn't pre-select the User Type dropdown, so the visitor has to pick `inquilino` manually.

**Verdict**

**Real code defect.** Fix shipped in commit `afdd58d` (this session).

**Action** (already done)

All three Services-section CTAs now route through `/register?role={propietario|inquilino|pymes}`:

```tsx
href="/register?role=propietario"  // Start Discovery Brief
href="/register?role=inquilino"    // Find your home
href="/register?role=pymes"        // Calculate your sales leak
```

The post-register redirect in `register/page.tsx` already maps role → form. So after Alex's fix is deployed:
- Visitor clicks any of those buttons → `/register` opens with User Type pre-selected
- Registers once → redirected to the right form ONCE with an active session
- Fills the brief once → submits → lead written, email fired, NO data loss

**Status**: ✅ **Verified by user 2026-06-02**. Incognito visit → Services section → "Start Discovery Brief" → landed on `/register?role=propietario` with User Type dropdown pre-selected to `propietario`. The form is NOT shown until after registration, eliminating the double-fill bug. Fix `afdd58d` confirmed working in production.

---

### #10 — "No están llegando los e-mail ni a comercial ni al cliente" (after payment)

**Symptom** (Alex, 1 Jun + 26 May, in the Stripe section)
> "Después de pagar no llego ningun email ni al cliente ni al comercial."

**Root cause**

**Duplicate of #02.** The Stripe webhook `checkout.session.completed` handler calls `sendPaymentReceiptEmail({ to: ctx.email, ... })`. The code is correct (verified in earlier rounds — see `33ff401` commit and earlier). Customer Gmail address silently drops because `RESEND_FROM_EMAIL` is still `WebMarketing <onboarding@resend.dev>`. Commercial side at `alexsanabria33@hotmail.com` typically arrives (Hotmail is more permissive) — Alex saying neither arrived is either a one-off webhook miss or she didn't refresh her Hotmail inbox.

**Verdict**

**Not a code defect.** Resolves automatically when #02 is fixed.

**Action**

No code change. See #02 for env-var update sequence.

**Status**: Analyzed — duplicate of #02. Will close when #02 is verified.

---

### #11 — "No hay campo para poder el código de descuento y promoción al momento de pagar"

**Symptom** (Alex, 1 Jun + 26 May)
> "No está el campo para colocar el codigo de descuento o promocion."
>
> Annotation on Stripe Checkout screenshots: *"Donde se pone el código de descuento o promoción?"*

**Root cause**

**Not a code defect, but a discoverability problem.** Promo input exists in TWO places after commit `cae88cf` (May 27):

1. **On our `/dashboard/services` page** — every CheckoutButton renders a "Have a promo code?" toggle below the Purchase button (see `src/components/checkout/checkout-button.tsx`, line 132-140). When clicked, it expands an input where the visitor types `WELCOME10` etc. The discount is applied BEFORE redirecting to Stripe.

2. **On the Stripe Checkout page itself** — `allow_promotion_codes: !promoCode` is set on the Checkout Session (`src/app/api/stripe/checkout/route.ts`, lines ~190 and ~310). Stripe displays an "Add promotion code" link near the Total. For it to work, the admin must ALSO create matching Promotion Codes in the Stripe Dashboard (Products → Coupons → Promotion Codes).

Why Alex didn't see either:

(a) Stripe's "Add promotion code" link is **collapsed by default** as a small text link near the totals. It's easy to miss. Once clicked, an input expands.
(b) Our own "Have a promo code?" toggle is below the purchase button on the PRE-Stripe page — Alex went directly to checkout and didn't scroll/look there.
(c) Even if the customer types `WELCOME10` in Stripe's native input, it will be rejected unless the same code is also defined inside Stripe Dashboard's Promotion Codes section. Our DB-side codes don't automatically sync to Stripe — they're applied via unit_amount reduction BEFORE the Stripe redirect.

**Verdict**

**Not a code defect.** Two complementary code paths exist. Documentation / training gap.

**Action**

No code change. Tell Alex:
- For testing now: enter `WELCOME10` on the `/dashboard/services` page in the "Have a promo code?" toggle BEFORE clicking the purchase button. The discount applies before Stripe.
- For Stripe-native codes (the field she's looking for on Stripe's checkout page itself): she must create matching codes in Stripe Dashboard → Products → Coupons → Promotion Codes. Each Stripe-native code is independent of our DB. This is intentional — gives her two separate workflows depending on which side she wants to manage.

**Status**: ✅ **Verified by user 2026-06-02**. Stripe Checkout page renders the native **"Add promotion code"** button on the left order summary, below the Subtotal line. `allow_promotion_codes` configuration confirmed working in production. Customers have TWO promo-input options: our own "Have a promo code?" toggle on the pre-Stripe `/dashboard/services` page (for DB codes like `WELCOME10`), and Stripe's native button on the checkout page itself (for codes created in Stripe Dashboard → Coupons → Promotion Codes). Two independent systems, both functional.

---

### #12 — "Donde el cliente pide una devolución de dinero? como es el proceso"

**Symptom** (Alex, 1 Jun + 26 May)
> "Donde el cliente pide una devolución de dinero? como es el proceso."

**Root cause**

**Already addressed in commit `33ff401`** (May 27). A help block was added to the bottom of `/dashboard/payments` explaining the Final Sale policy and providing a contact email for exceptional cases. Alex may not have scrolled to the bottom of the page or may be testing on a stale browser.

**Verdict**

**Not a current code defect.** Fix already shipped in `33ff401`.

**Action**

No new code change. Alex to verify: sign in as a customer → `/dashboard/payments` → scroll to bottom → confirm the "Need help with a payment?" panel renders with a `mailto:` link to `partners@nexuma.ca` (or whatever `NEXT_PUBLIC_CONTACT_EMAIL` is set to).

**Status**: ✅ **Verified by user 2026-06-02**. /dashboard/payments renders the help block at the bottom of the page: "Need help with a payment? ... contact our team at partners@nexuma.ca. Please include your payment date and amount. Our team will review your case within 2 business days. Per our policy, all sales are final once the service period has started. Refunds are considered only for exceptional circumstances (technical errors, duplicate charges)." Fix `33ff401` confirmed working in production.

---

### #13 — "Está dejando pagar pero no crea el cliente ni queda en el panel administrador" (alexsmarke@gmail.com)

**Symptom** (Alex, 1 Jun + 26 May)
> "Está dejando pagar pero no crea el cliente ni queda en la el panel administrador ver el caso documentado arriba del cliente alexsmarke@gmail.com."

**Root cause**

**Combination of #01, #06, and #09.** Three things layered:

1. `alexsmarke@gmail.com` is an `auth.users` row left over from earlier testing — see #01 for the 3-table explanation (`auth.users` vs `profiles` vs `leads`).
2. If Alex clicked the lower Services-section CTA "Start Discovery Brief" instead of the top hero button, the broken flow in #09 kicks in: registration completes, payment processes via Stripe, but the form is asked to be filled again post-register, never gets submitted, so `/api/leads` is never called and no lead row is written. Hence the "no client in admin panel" complaint.
3. After #09's fix (commit `afdd58d`), this path now writes the lead correctly because the visitor lands on the form ONCE with an active session, submits it, and `/api/leads` runs as part of the submit handler.

**Verdict**

**Resolved by #09's fix** (`afdd58d`). The leftover `alexsmarke@gmail.com` rows are a separate data-cleanup item (see #01).

**Action**

No new code change. Verification path for Alex:
1. Wait for Vercel to deploy `afdd58d` (~60s after push).
2. Open incognito → click "Start Discovery Brief" from the Services section (lower on the page, NOT the top hero button) → register with a new email → complete the brief → submit.
3. As admin → `/admin/leads` → confirm the new lead row appears with the new email.

**Status**: ✅ **Verified by user 2026-06-02 via #09**. With the service-card CTA fix verified working (see #09), this composite bug is closed by the same fix. Future visitors going through the Services section will register before form-fill, so leads will be written and visible in `/admin/leads`.

---

### #14 — Founders Package CTA works but "no está sumando los impuestos"

**Symptom** (Alex, 26 May, reiterated 1 Jun)
> "Probe desde el perfil de propietario comprar el Founders Package y el CTA o botón ya sirve, pero no está sumando los impuestos?"
>
> Screenshot annotation: *"Porque NO suma el impuesto?"* pointing at `Impuesto CAD0.00` on the Stripe Checkout page.

**Root cause**

**Not a code defect.** Stripe Tax is correctly opted in via `automatic_tax: { enabled: true }` and `tax_behavior: "exclusive"` on every line item (commit `f0d951d` from May 20). For Stripe to actually charge 5% GST, the merchant must have an **active Tax Registration** in Stripe Dashboard → Tax → Registrations for the relevant jurisdiction (Canada → Federal).

Per Alex's own 2026-05-20 confirmation (logged in `feedback/MILESTONE4_FINAL_DECISIONS.md` §2):

> "GST registration number: NOT YET ISSUED — client is applying with CRA / GST number on receipts: Leave blank/disabled for now; client will add to Stripe Dashboard once CRA assigns."

So the Stripe Checkout page correctly shows `CAD 0.00` impuesto right now. The moment Alex adds her CRA-issued GST number in Stripe Dashboard → Tax → Registrations, the same checkout will start showing `CAD X.XX` GST automatically — no code change needed.

**Verdict**

**Not a code defect. Config gap — client-side, blocked on CRA issuing the GST registration number.**

**Action**

No code change. When CRA issues the GST number, Alex does this in Stripe Dashboard (5 minutes):

1. Stripe Dashboard → Settings → Tax → Registrations → + Add registration
2. Country: Canada / Province: Federal / Registration number: paste GST# (format `123456789 RT 0001`)
3. Save

After that the 5% GST appears on every checkout automatically.

**Status**: Analyzed — blocked on external CRA issuance. No code change required.

---

### #15 — "Enlaces Legales en el Footer del carrito de compras"

**Symptom** (Alex, 1 Jun, reiterated)
> "Enlaces Legales en el Footer del carrito de compras (Términos y la Política de Privacidad tengan su link en la web) Pendiente 1 Jun: No vi este al momento de pagar."

Alex re-pasted the exact bilingual refund / cancellation policy text she previously emailed and wants those linked from inside the Stripe Checkout page footer.

**Root cause**

**Already addressed in commit `cae88cf`** (May 27). The Stripe Checkout sessions are created with:

```ts
consent_collection: { terms_of_service: "required" },
custom_text: {
  terms_of_service_acceptance: {
    message: "I agree to the [Terms of Service](...) and [Privacy Policy](...). All sales are final per our [Refund Policy](...).",
  },
},
```

This forces the customer to tick "I agree to the Terms" before Stripe lets them pay. The links inside go to `/legal/terms_of_service`, `/legal/privacy_policy`, and `/legal/refund_policy` on the live site.

Two reasons Alex may have missed it:

1. Stripe's consent checkbox + custom text appears just above the Pay button — it can be off-screen on smaller viewports until the customer scrolls.
2. If Alex tested BEFORE May 27 (when `cae88cf` deployed), the checkout was the older version without consent_collection. The 1 Jun report doesn't say which test session she's referring to.

Separately, the `/legal/refund_policy` content itself (the bilingual Spanish-English text Alex re-pasted) was seeded via migration `v31` earlier this month. So clicking the link from inside Stripe Checkout brings the customer to a page with her exact wording.

**Verdict**

**Not a current code defect.** Fix already shipped in `cae88cf` with the exact link Alex wants.

**Action**

No new code change. Alex to verify on a fresh checkout session after Vercel has deployed `cae88cf` (already deployed by 1 Jun):
1. Sign in as a customer → start a checkout → scroll all the way down on the Stripe Checkout page → confirm the "I agree to the Terms of Service and Privacy Policy. All sales are final per our Refund Policy" line appears with three blue clickable links.
2. Click each link → verify it opens the correct policy page in a new tab.

**Status**: ✅ **Verified by user 2026-06-02**. Stripe Checkout page renders the consent area exactly above the Pay button with the configured text: *"I agree to the Terms of Service and Privacy Policy. All sales are final per our Refund Policy."* All three legal terms appear as blue underlined clickable links. `consent_collection.terms_of_service = "required"` + `custom_text` configuration confirmed working in production. Customer must check the box before Stripe lets them pay, satisfying Alex's "No vi este al momento de pagar" complaint.

---

### #16 — Item 8 Promotions / Campaigns: "no vi donde crearlo" + "Donde se elimina"

**Symptom** (Alex, on Item 8 — Gestión de Precios y Promociones)
> ➔ Crear promociones temporales — *"NO hay campo al momento de pagar donde poder aplicar el descuento con el codigo WELCOME10"*
>
> *"Se puede crear el descuento o promoción (e.g. hice un descuento llamado POR CREER) pero no los pude probar, por lo dicho arriba"*
>
> *"Donde se cancelan o eliminan los descuentos? eso falta, solo está desactivar pero si se quiere eliminar dónde es?"*
>
> ➔ Crear campañas o promociones por: Temporada / Zona / Tipo de cliente — *"No vi donde crearlo"*
>
> ➔ Definir fechas de inicio / fin — *"No vi donde crearlo"*
>
> *"Por favor Steve decirme donde creó las campanas? solo vi las promociones y si se crean en articles decirme como?"*

Screenshot of the New/Edit Promotion modal includes a handwritten annotation **"Donde se elimina?"** (Where do you delete?) pointing at the Cancel/Update button area.

**Root cause**

**All sub-items are duplicates of previously-fixed work.** Crucially, the screenshot timestamp is **26 May** — every fix below shipped on or after **May 27**, so Alex was testing the OLD UI:

| Sub-complaint | Already-shipped fix | Commit |
|---|---|---|
| "No hay campo al momento de pagar" (promo on Stripe page) | `allow_promotion_codes` on Stripe Checkout sessions | `cae88cf` (5/27) — see #11 |
| "Hice POR CREER pero no los pude probar" | Same as above + CheckoutButton's "Have a promo code?" toggle on `/dashboard/services` | `cae88cf` + earlier (#11) |
| "Donde se cancelan o eliminan los descuentos? solo está desactivar" | Added a red **Delete** button next to Edit on every promotion row, with confirmation dialog | `7191eab` (5/27) |
| "Temporada — No vi donde crearlo" | Added a labeled blue panel "**📅 Temporada / Validity dates**" wrapping the start/end date inputs in the modal, with explanatory text ("Set start/end dates to make this a seasonal campaign") | `ae3f11b` (5/27) |
| "Zona — No vi donde crearlo" | Already inside an amber "Promotion targeting (Zona / Tipo de cliente)" panel with a Zona input — was below the modal fold; modal made scrollable + section index added to header | `ae3f11b` (5/27) |
| "Tipo de cliente — No vi donde crearlo" | Already inside same amber panel as chip selectors | `ae3f11b` (5/27) |
| "Definir fechas de inicio / fin — No vi donde crearlo" | Same as Temporada — those date fields are EXACTLY what the new blue panel labels as the seasonal/Temporada control | `ae3f11b` (5/27) |
| "Activar/desactivar descuentos" | Already worked. Alex marked **"Ajustado"** | (pre-existing) |
| "Donde creó las campanas? solo vi las promociones" | Card title renamed to **"Promotions / Campaigns"** with description explaining that targeting fields (Zone + Tipo de cliente + dates) ARE the campaign feature — there is no separate "Campaigns" section to look for | `7191eab` (5/27) |

The screenshot Alex attached actually CONTAINS the labeled "Temporada / Validity dates" panel and the Zona / Tipo de cliente section — she captured them in frame but still wrote "No vi donde crearlo", which suggests either (a) the screenshot was taken AFTER the 5/27 deploy but the complaint text was carried forward from the older 5/26 report without re-checking, or (b) the section labels (English + Spanish hybrid like "Temporada / Validity dates") didn't register to her as the seasonal-campaign feature.

Regarding "y si se crean en articles decirme como?": Alex briefly wondered if campaigns are managed under `/admin/articles`. They are not — Articles is the blog / resources system. Promotions/Campaigns are exclusively at `/admin/pricing`.

**Verdict**

**Not a current code defect.** Every concrete sub-item already shipped before 5/27. The remaining gap is purely a communication one — Alex needs to re-test on the live deploy (commit `7191eab` is from 5/27 and is definitely live by 6/1).

**Action**

No new code change. Walk Alex through ONE concrete end-to-end test on the current deploy:

1. **Hard-refresh** `/admin/pricing` (Ctrl+Shift+R) to bypass any stale cache.
2. Promotions section title now reads **"Promotions / Campaigns"** with a description that explains targeting = campaigns.
3. Find any existing promo row → there are now TWO buttons in the Action column: an outlined **Edit** and a red ghost **Delete**. Click Delete → a confirm dialog asks "Delete promotion ... permanently? This cannot be undone." → click OK → the row disappears.
4. Click **+ New Promotion** → the dialog opens with a Sections index at the top: "Basic info · Validity dates (Temporada) · Max uses · Targeting (Zona + Tipo de cliente) · Active toggle. Scroll down to see them all." Scroll past the basic fields to find:
   - Blue panel titled **"📅 Temporada / Validity dates"** with the Valid From / Valid Until pickers — THIS is the seasonal control.
   - Amber panel titled **"Promotion targeting (Zona / Tipo de cliente)"** with a Zona input + role chips — THIS is zone + customer-type targeting.
5. For the Stripe-side promo input (#11), enter `WELCOME10` BEFORE the Stripe redirect — on the CheckoutButton's "Have a promo code?" toggle below the Purchase button on `/dashboard/services`.

**Status**: ✅ **Verified by user 2026-06-02**. /admin/pricing → Promotions / Campaigns card now reads "Promotions / Campaigns" with description listing date range (Temporada), city (Zona), customer type (Tipo de cliente). Every row in the Action column has both Edit and red Delete buttons. Counter on WELCOME10 shows `2 / 100` proving the promo-counter increment fix works in production. All sub-items confirmed resolved. Fixes `ae3f11b` + `7191eab` + `cae88cf` + `44caad2` all confirmed in production.

---

### #17 — "Cobranzas" cleanup: verify in Supabase database

**Symptom** (Alex, on Item 9)
> "Pendiente 1 jun verificar en base de datos Supabase ya está ajustado en panel administrador."

Translation: she has visually confirmed in the admin panel that the "Cobranzas" (payments / billing) test data is cleaned up, but she wants to verify it directly in the Supabase database before final delivery.

Context from earlier docx revisions:
- 22 May: marked "Pendiente"
- 26 May: marked **"Ajustado"** (already cleaned)
- 1 Jun: confirmed clean in admin UI; now wants DB-level verification

**Root cause**

**Not an error — a verification request.** The `payments` table currently holds a small number of test rows from the end-to-end Stripe + refund testing rounds (e.g., the `$94.50 completed` and `$100 refunded` rows). Those are real test transactions that were intentionally left in place to validate the refund webhook and the `/admin/payments` flow.

Three reasonable interpretations of what Alex wants:

1. **Confirm the DB matches what the UI shows.** Run two SELECTs and compare row counts.
2. **Clear ALL test payments before production launch.** Truncate the `payments` table.
3. **Clear only the older test-mode payments** but keep recent verifications. This requires a date threshold or a `stripe_payment_intent_id` filter.

The current docx language ("Ajustado en panel administrador") leans toward interpretation 1 — Alex sees a clean panel and wants to confirm the underlying DB matches. No deletion needed, just verification.

**Verdict**

**Not a code or data defect.** No SQL has been issued. The pre-launch full clear is a separate joint task to schedule with Item 10 (domain switch) so test data is wiped right before the production cutover, not before.

**Action**

No code change. Recommend running these SQL queries in Supabase SQL Editor to give Alex the proof she's asking for:

```sql
-- 1. Total payment rows + breakdown by status
SELECT status, COUNT(*) AS n, SUM(amount) AS total
FROM payments
GROUP BY status
ORDER BY status;

-- 2. List all payments with the related user (joins via service_role since
-- /admin/payments uses the same /api/admin/payments service-role join)
SELECT
  p.created_at::date AS date,
  pr.email AS user_email,
  pr.full_name AS user_name,
  COALESCE(s.name, pp.name, '—') AS service_or_plan,
  p.amount,
  p.currency,
  p.status,
  p.stripe_payment_intent_id
FROM payments p
LEFT JOIN profiles pr ON pr.id = p.user_id
LEFT JOIN services s ON s.id = p.service_id
LEFT JOIN pymes_plans pp ON pp.id = p.pymes_plan_id
ORDER BY p.created_at DESC
LIMIT 50;
```

If Alex confirms the rows shown match what /admin/payments displays → DB and UI are consistent, verification complete.

For the actual pre-production wipe (interpretation 2), defer to the joint handover session — that's the right time to do `DELETE FROM payments` cleanly so we don't accidentally remove rows that production starts producing in the interim.

**Status**: ✅ **Verified by user 2026-06-02**. SQL query results match `/admin/payments` exactly: 5 `completed` rows totaling $890, 1 `refunded` row at $100, 6 transactions total. Two Founders Package rows match `foundersCounter.taken = 2` in `/api/health`, proving the increment webhook fires correctly. DB and UI consistent. No data cleanup needed before joint handover session.

---

### #18 — Domain switch from `webmarketing-lyart.vercel.app` to `nexuma.ca`

**Symptom** (Alex, on Item 10 — also pending since 22 May, 26 May, and 1 Jun)
> "Al estar listos para conectar Cloudflare, por favor configurar el entorno de producción directamente en nuestro dominio raíz definitivo nexuma.ca. Actualmente en la dirección web se ve: https://webmarketing-lyart.vercel.app"

Screenshot annotation is a red arrow pointing at the URL bar showing `webmarketing-lyart.vercel.app/admin/images`.

**Root cause**

**Not a code or data issue. Deployment/infrastructure task.** The current production URL is the default Vercel-assigned subdomain. To serve the same Next.js app from `nexuma.ca`:

1. **In Vercel** — Project → Settings → Domains → Add `nexuma.ca` and `www.nexuma.ca`. Vercel displays the required DNS records (typically an A record `76.76.21.21` for the root and a CNAME `cname.vercel-dns.com` for `www`).
2. **In Cloudflare** — `nexuma.ca` zone → DNS → add those records exactly as Vercel showed. For initial verification the Proxy must be **DNS only (gray cloud)**; can be re-enabled to Proxied (orange) after Vercel confirms.
3. **TLS certificate** — Vercel auto-provisions Let's Encrypt once DNS resolves. Usually 1-3 minutes.
4. **App-side** — set Vercel env var `NEXT_PUBLIC_APP_URL` to `https://nexuma.ca` and redeploy. The hardcoded fallback URLs in email helpers (`https://webmarketing-lyart.vercel.app`) only apply when the env var is unset; once set, all outbound links in emails / Stripe success URLs / legal-doc links inside Stripe Checkout will switch to the new domain automatically.

Steps 2 and 4 require both Tony and Alex in the same session: Alex owns Cloudflare DNS, Tony owns Vercel env vars.

**Verdict**

**Not a code defect. Joint session task — same session as #08 (Supabase) and Item 11a-e (other ownership transfers).**

**Action**

No code change. To address in the joint handover session:

1. Tony adds `nexuma.ca` + `www.nexuma.ca` in Vercel → Vercel surfaces the required A / CNAME values.
2. Alex pastes those values into Cloudflare DNS (Proxy = DNS only).
3. Wait for green checkmarks in Vercel (1-3 min).
4. Tony sets `NEXT_PUBLIC_APP_URL=https://nexuma.ca` in Vercel env vars (Production + Preview + Development) and redeploys.
5. Hard-refresh `https://nexuma.ca` → confirm the Nexuma Marketing landing page loads under the new domain.
6. As Tony's last step before closing the session: in Stripe Dashboard → Webhooks, also point the webhook endpoint at `https://nexuma.ca/api/stripe/webhook` instead of the vercel.app URL (the old URL can remain as a backup for a few days).

Until that session: the app continues to function on `webmarketing-lyart.vercel.app`. The vercel.app URL will keep working even after the cutover — Vercel does not auto-redirect, so if Alex wants the old URL to bounce to nexuma.ca that needs to be configured as a separate Vercel redirect rule.

**Status**: Analyzed — joint-session task. No code change required. Closes when handover session is held.

---

### #19 — Item 11: Final deliverables (a–f)

**Symptom** (Alex, on Item 11 — pending since 22 May, re-stated 1 Jun verbatim)

> a) Código fuente: Transferir la propiedad del "repositorio" de código
> b) Hosting (Vercel): La cuenta de Vercel debe estar conectada a mis datos
> c) Base de Datos (Supabase): Debes tener el usuario administrador principal de Supabase
> d) Dominio y DNS (Cloudflare): Como ya es mío, por favor garantizar que no tienes contraseñas
> e) Stripe: Confirmar que las llaves pasaron de test (prueba) a live (producción) y que funcionan
> f) Manual: Entregar un archivo de texto sencillo que explique: tecnologías, cambio de precios, arquitectura, diccionario de datos Supabase, variables de entorno, manual operativo (precios + equipos + campañas por zona/tipo)

**Root cause**

**Not a code issue. Operational/handover checklist.** Six discrete tasks, each in a different state:

| Sub-item | What it requires | Current state |
|---|---|---|
| **a)** GitHub repo transfer | GitHub Settings → Transfer ownership → enter Alex's username/org | Pending — Alex must create a GitHub account first and share the username |
| **b)** Vercel project transfer | Vercel Project Settings → General → Transfer to a different team/account | Pending — Alex must create a Vercel account (free tier is fine), link it to her GitHub, and share the team slug |
| **c)** Supabase admin transfer | Supabase project owner re-assignment OR invite Alex as Owner | Pending — see #08. Alex must create a Supabase account first; the project transfer happens in the same session |
| **d)** Cloudflare — confirm no passwords with Tony | Verification only | **Already true** — Tony never received or stored Cloudflare credentials. Alex set the DNS records herself when verifying `nexuma.ca` in Resend (entry #02 sequence). To formally close: Tony confirms this to her in writing |
| **e)** Stripe test → live activation + key swap | Alex completes Stripe identity/business verification in her own Stripe Dashboard, generates live `sk_live_...` and `pk_live_...` keys, sends them to Tony to paste into Vercel env vars, then Tony swaps webhook signing secret to the live mode value | Pending — see also #14 (CRA GST# is a parallel pre-requisite for tax to actually charge) |
| **f)** Manual covering tech/pricing/arch/DB/env/ops | All six sub-bullets documented in `HANDOVER_MANUAL.md` at repo root | **Already delivered.** §1 Tech Stack, §3 Changing plan prices, §4 Architecture map, §5 Supabase data dictionary, §6 Environment variables, §8 Operations cookbook (Promotions/Campaigns + Team management). New `property_images` Storage subsection added in `ae3f11b` |

**Verdict**

**Sub-item (d) is already true; sub-item (f) is already delivered.** Sub-items (a), (b), (c), (e) are all joint-session tasks blocked on Alex creating accounts on the destination platforms (GitHub, Vercel, Supabase) and completing Stripe identity verification.

**Action**

No code change. To address in the joint handover session (same session as #08 + #18):

**Send to Alex before the session:**

> Para la sesión de entrega final necesito que tengas listas estas cuentas (todas gratis):
>
> 1. **GitHub** — crear cuenta en [github.com/signup](https://github.com/signup), enviarme tu username
> 2. **Vercel** — crear cuenta en [vercel.com/signup](https://vercel.com/signup) usando tu cuenta de GitHub, enviarme el nombre del team
> 3. **Supabase** — crear cuenta en [supabase.com](https://supabase.com), enviarme el email registrado
> 4. **Stripe Live Mode** — completar la verificación de identidad de negocio en [dashboard.stripe.com/account/onboarding](https://dashboard.stripe.com/account/onboarding) (necesitan tu información fiscal, dirección legal, cuenta bancaria, identificación). Cuando Stripe te apruebe el modo Live, generar las llaves Live (sk_live_... y pk_live_...) desde Developers → API Keys
>
> Sobre Cloudflare (sub-item d): confirmo por escrito que **nunca tuve acceso a tu cuenta de Cloudflare**. Los registros DNS para Resend los configuraste tú directamente desde tu cuenta. No hay contraseñas ni accesos compartidos.
>
> El manual operativo (sub-item f) ya está en el repositorio como `HANDOVER_MANUAL.md`. Te lo enviaré junto con la captura de las secciones que pediste.

**During the session:**

1. (a) Tony: GitHub repo → Settings → Transfer → Alex's username → Alex accepts the transfer request.
2. (b) Tony: Vercel project → Settings → General → Transfer → Alex's team → Alex accepts.
3. (c) Tony: Supabase project → Settings → invite Alex as Owner → Alex accepts → Tony demotes himself to read-only (or removes).
4. (d) Already-clean: just confirm.
5. (e) Alex pastes live keys into a private channel → Tony updates `STRIPE_SECRET_KEY` + `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` + `STRIPE_WEBHOOK_SECRET` in Vercel env vars → redeploys → verifies via `/api/health` that `stripe.secretKeyMode === "live"`.
6. (f) Walk through `HANDOVER_MANUAL.md` sections live so Alex knows where everything is.

**Status**: Analyzed — joint-session task (a, b, c, e) + already-done verification (d) + already-delivered manual (f). No code change required.

---

### #20 — Vercel builds silently failing since May 27 → production stuck on `3dd6278`

**Symptom** (discovered via `/api/health` output 2026-06-02)

`/api/health` returned `"commit": "3dd6278"` even though `git log origin/main` shows 4 newer commits pushed and acknowledged by GitHub:
- `4d27a79` (rebrand)
- `f54a07a` (#04b register-first + #04a hero CTA labels)
- `33ff401`, `cae88cf`, `7191eab`, `ae3f11b`, `da0677d`, `44caad2`, `baf5a6b`, `b50e780`, `7ea4bcc` (other fixes)
- `fd7a641` (#05 hero CTA admin fields)
- `afdd58d` (#09 service-card CTAs)

Production hadn't moved past `3dd6278` since 5/27. Every Part A test of the 6-2.md log was therefore being run against stale code.

Vercel Deployments tab → most recent failed build showed the actual root cause:

```
useSearchParams() should be wrapped in a suspense boundary at page "/register"
Error occurred prerendering page "/register"
Export encountered an error on /(auth)/register/page: /register, exiting the build.
Next.js build worker exited with code: 1
Error: Command "npm run build" exited with 1
```

**Root cause**

**Real code defect introduced in `f54a07a`** and propagated through every subsequent commit.

When `f54a07a` added the `?role=` query-param read to the register page, it called `useSearchParams()` at the top level of a client component:

```tsx
"use client";
// ...
export default function RegisterPage() {
  const searchParams = useSearchParams();  // <-- here
  const preselectedRole = searchParams.get("role") || "";
  // ...
}
```

Next.js 16 statically prerenders `/register` at build time. During prerender, the URL is not yet known, so `useSearchParams()` returns a Suspense throw — which the framework requires you to catch with a `<Suspense>` boundary so the param-dependent subtree can be deferred until request time.

Without the boundary, the prerender throws, the static-page export fails, and `next build` exits with code 1. Vercel correctly aborts the deployment. The same file pattern (`reset-password/page.tsx`) already used `<Suspense>` correctly — the boundary was simply missed when register's hook was introduced.

Effect: every commit pushed after `f54a07a` got queued, attempted, failed, and rolled back to the last successful build — which was `3dd6278`. So 12 commits' worth of fixes (#05, #09, #10's pre-Resend bits, all rebrand defaults, etc.) accumulated unreleased.

**Verdict**

**Real code defect.** Fix shipped in commit `5e3598e` (this session).

**Action** (already done)

`src/app/(auth)/register/page.tsx`:
1. Imported `Suspense` from `react`.
2. Renamed the existing `RegisterPage` function body to `RegisterPageContent` — still uses `useSearchParams()`.
3. Added a new `RegisterPage` default export that wraps `RegisterPageContent` in `<Suspense fallback={null}>`.

This is exactly the pattern `src/app/(auth)/reset-password/page.tsx` already uses for the same reason.

**Once Vercel succeeds with `5e3598e`:**

Production will jump from `3dd6278` all the way to `5e3598e` in one deploy. Every fix from `4d27a79` through `afdd58d` will go live at the same time. Then:

- Part A.1 (#05) starts passing — the rebrand + the admin/content hero CTA fields are live
- Part A.2 (#09, #13) starts passing — the service-card CTAs go through register first
- The brand name in fallback contexts (email FROM string, page title) reads "Nexuma Marketing"

Vercel build should take ~60-90 seconds. After it succeeds, re-check `/api/health` → confirm `commit` is now `5e3598e` (or later).

**Status**: Fix shipped in `5e3598e` — awaiting Vercel build success + client verification.

---

### #22 — Admin layout red "RLS will deny" banner shown to marketing/sales/support

**Symptom** (Alex, 2026-06-04 Item 4, screenshots 4, 5 and Item 5)

> Created `marketing26@zohomailcloud.ca` as a marketing user. Logged in as that user, the screen showed two banners stacked:
> 1. Blue: *"Signed in as marketing ... limited access"*
> 2. Red: *"⚠ You are not signed in as an admin. Postgres RLS will silently deny every save you attempt — buttons will appear to succeed but the database will not change."*
>
> Below that the Lead Management list was empty (a separate issue).

**Root cause**

`src/app/(dashboard)/admin/layout.tsx` rendered the red warning whenever `isAdmin` was false. The earlier intent (from May 13 incident analysis) was to scare a `propietario` who navigated into `/admin/*` and would otherwise click Save into the void. But the same condition fires for legitimate `marketing` / `sales` / `support` users, whose RLS in v9 actually allows writes to their scoped tables.

Net effect: every team member Alex created saw a screen that contradicted itself — blue banner said "you have limited access", red banner said "every save will silently fail". She concluded the team feature was broken.

**Verdict**

**Real code defect.** Fix shipped in commit `f99094c`.

**Action**

Changed the ternary so the red warning only fires for `!isInternalRole` (truly outside the team — propietario / inquilino / pymes / NULL). Blue banner continues to cover marketing / sales / support. Admin still gets the positive confirmation banner.

**Status**: Fix shipped in `f99094c` — awaiting client verification.

---

### #23 — /admin/properties Owner column shows "Unknown" for every row

**Symptom** (Alex, 2026-06-04 Item 4, Screenshot 7)

The Property Management table showed `Unknown` in the Owner column for every row. Some rows DID have valid owners (e.g. row 5 in the export was "Alex Sanabria"). The owner export CSV had the same gap — only Alex's row had data, the rest were blank.

**Root cause**

`/admin/properties/page.tsx` used the cookie-context Supabase client with an embedded join `profiles:owner_id(full_name)`. Same family of bug as the original `/admin/payments` "Unknown user" issue (`#44caad2`): PostgREST silently returns `null` for the embed when RLS interactions across the schema don't resolve cleanly, leaving the page with no owner data to display.

**Verdict**

**Real code defect.** Fix shipped in commit `f99094c`.

**Action**

New server route `/api/admin/properties` authenticates the caller as an internal role, then uses `supabaseAdmin` to fetch properties + manually join the `profiles` rows for the involved `owner_id`s. Page now renders Owner Name + Owner Email pair in the Owner column.

**Status**: Fix shipped in `f99094c` — awaiting client verification.

---

### #24 — Lead status dropdown only exposes "In Progress" and "Closed"

**Symptom** (Alex, 2026-06-04 Item 4, Screenshot 6)

> "No están todos los status en lead management, son nuevo-contactado-en proceso y cerrado. solo sale en proceso y cerrado."

In the Manage Lead modal, the Status dropdown for a lead currently at `en_proceso` only listed "In Progress" and "Closed". `nuevo` and `contactado` were hidden, so a sales operator could not revert a misclick.

**Root cause**

Two layers enforced a one-way workflow:

1. `admin/leads/page.tsx` computed `allowedStatuses = [selectedLead.status, ...LEAD_STATUS_TRANSITIONS[selectedLead.status]]`. With `en_proceso → [cerrado]`, the dropdown showed only those two.
2. `migration_v8_phase3_polish.sql` installed a Postgres trigger `enforce_lead_status_transition` that raised an exception on any "illegal" transition (e.g. `cerrado → contactado`).

Client expected ANY-to-ANY status changes per their business workflow.

**Verdict**

**Real defect — by-spec change.** Fix shipped in commit `f99094c` + migration `v35`.

**Action**

- `admin/leads/page.tsx`: `allowedStatuses` now returns all 4 keys of `LEAD_STATUS_LABELS`. Client-side transition validation removed from `handleStatusUpdate`.
- `migration_v35_relax_lead_status_transitions.sql`: drops both the trigger and the function so DB no longer rejects manual reverts. Run as postgres / service_role.

**Status**: Fix shipped in `f99094c`; **v35 must be run in Supabase SQL Editor** before client verification.

---

### #25 — Service Reassignment "Pick a client" shows 0 clients

**Symptom** (Alex, 2026-06-04 Item 4, Screenshot 8)

`/admin/reassign` rendered the "Pick a client" card with `0 clients` and the message *"No clients match"*. Searching by email (e.g. `produccionulf@gmail.com`, which definitely exists in `profiles`) returned the same empty state.

**Root cause**

The page's `loadInitial` ran two cookie-context queries (`profiles.in("role", [...])` and `services.or(...)`). The profiles query returned `[]` even for admin. Most likely the same admin SELECT recursion quirk that's already been working around the rest of the surface, but the exact failure mode is irrelevant — service-role bypasses it.

**Verdict**

**Real code defect.** Fix shipped in commit `f99094c`.

**Action**

New `/api/admin/reassign` route authenticates internal roles, then uses `supabaseAdmin` to return `{ users, services }`. Page now fetches that JSON instead of calling Supabase directly.

**Status**: Fix shipped in `f99094c` — awaiting client verification.

---

### #26 — Image Library search by owner name returns no results

**Symptom** (Alex, 2026-06-04 Item 4 + Item 5 attached screenshots)

`/admin/images` displayed all uploaded photos by default. Typing an owner's display name (e.g. `inversor test milestone 4`) into the search box collapsed the result set to "No images match your filters", even when an admin separately confirmed in `/admin/leads` that that owner existed.

**Root cause**

The page joined `property_images` to `properties` to `profiles` across two queries — the embed `properties:property_id(...)` plus a second `profiles.in("id", ownerIds)` query. The embed could silently drop the property row, taking the owner reference with it; or the second profile fetch could return nothing under RLS. Either way the in-memory map keyed by `owner_id → owner` ended up empty, so the search haystack never contained owner names.

**Verdict**

**Real code defect.** Fix shipped in commit `f99094c`.

**Action**

New `/api/admin/images` route uses service-role to fetch property_images → properties → profiles via 3 explicit `IN()` lookups and returns each image enriched with `property` and `owner` objects. Search now matches against `owner.full_name` reliably.

**Status**: Fix shipped in `f99094c` — awaiting client verification.

---

### #27 — Newly created team members don't appear in /admin/team

**Symptom** (Alex, 2026-06-04 Item 4, Screenshots 3, 4, 5)

After clicking "Add team member" and submitting, the green success banner said *"Created marketing26@zohomailcloud.ca as marketing"*. But the Team Members table still showed only Alex's admin row. Refreshing the page didn't help. The newly created user could log in (proving the auth row existed), but Alex couldn't assign leads to them because they didn't appear in dropdowns.

**Root cause**

Same cookie-context profile read returning under-populated data. The roster query was scoped to `role IN ("admin","marketing","sales","support")` but returned only the admin row.

**Verdict**

**Real code defect.** Fix shipped in commit `f99094c`.

**Action**

New `/api/admin/team` route (admin-only, more restrictive than other admin endpoints since this exposes the staff roster). Page fetches from there. Newly created users appear immediately after `load()` re-runs.

**Status**: Fix shipped in `f99094c` — awaiting client verification.

---

### #28 — Sales role sees CA$0 / 0 transactions in Sales Report

**Symptom** (Alex, 2026-06-04 Item 5 second screenshot)

Signed in as a sales user, `/admin/reports` showed all 4 metric cards at `CA$0`, even though admin saw `$890 revenue / 6 transactions / 1 refunded` for the exact same period. The lead funnel and promo redemption sections were also empty.

**Root cause**

Two of the four tables the page queried (`payments`, `promotions`) have admin-only RLS by design. The cookie-context client running as `sales` correctly received zero rows. Same for `/admin/payments`, which had a hard `role !== "admin"` gate at the API layer.

**Verdict**

**Real code defect — by-spec extension** of internal-role access. Fix shipped in commit `f99094c`.

**Action**

- New `/api/admin/reports` route uses `supabaseAdmin` to bypass the admin-only RLS on payments and promotions. Allowed to all internal roles (sales legitimately needs the numbers; marketing wants promo statistics; support can already read at the page level).
- `/api/admin/payments` widened from admin-only to all internal roles. The state-mutating `POST /api/admin/stripe/refund` and `POST /api/admin/stripe/cancel-subscription` endpoints stay admin-only — sales can READ but not REFUND/CANCEL.

**Status**: Fix shipped in `f99094c` — awaiting client verification.

---

### #29 — CSV exports missing key columns (Customer Name, Plan, Owner contact)

**Symptom** (Alex, 2026-06-04 Item 4, Screenshots 1 and 2)

> Screenshot 1 (`payments_export_2026-06-05.xlsx`): only User Email, Amount, Currency, Type, Status, Date columns. Annotation: *"Quién pagó? Name is missing, Direction, Plan"*.
>
> Screenshot 2 (`properties_export_2026-06-04.xlsx`): Owner Name / Owner Email / Owner Phone columns existed but were blank for 3 of 4 rows. Only Alex's own row had data. Annotation: *"No trae el dato"*.

**Root cause**

Two compounding issues:

1. The payments CSV column definitions (`COLUMN_DEFS.payments`) only listed 6 columns. Customer Name and Plan were intentionally omitted historically.
2. The page used cookie-context PostgREST embeds for properties (`owner:owner_id(full_name, email, phone)`), payments (`profiles:user_id(email)`), and leads (`profiles:user_id(role)`). The embeds silently returned null for the same RLS-interaction reasons #23 / #26 hit. Even where the embed did resolve, the missing column definitions meant the export omitted the data even when fetched.

**Verdict**

**Real code defect.** Fix shipped in commit `f99094c`.

**Action**

- New `/api/admin/export` route handles all four export targets server-side with `supabaseAdmin`. Performs explicit `IN()` joins for owners (properties), customers + service names + plan names (payments), and profile roles (leads). Returns the rich rows.
- Page now POSTs to that route and runs `generateCSV` on the response.
- `COLUMN_DEFS.payments` extended with `user_name` ("Customer Name") and `plan_or_service` ("Plan / Service") columns at the front.
- Date and role filters move with the request as query params; preserves the original UI.

**Status**: Fix shipped in `f99094c` — awaiting client verification. After Vercel deploys, Alex's next payments export will include a populated Customer Name + Plan column for every row.
